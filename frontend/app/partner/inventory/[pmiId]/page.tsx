// Placeholder — satisfies static export. Real content is served from /partner/inventory?pmi=
export function generateStaticParams() {
  return [{ pmiId: '_' }];
}
export default function PMIDetailPlaceholder() { return null; }
